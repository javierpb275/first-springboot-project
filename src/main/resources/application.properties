info.app.name=first springboot project
info.app.description=my first springboot app
info.app.version=1.0.0

# Use wildcard "*" to expose all endpoints
#Can also expose individual endpoints with a comma-delimited list
#
management.endpoints.web.exposure.include=*

#Exclude individual endpoints with a comma-delimited list
#management.endpoints.web.exposure.exclude=health,info

#
#Define my properties
#
coach.name=Mickey Mouse
team.name=The Mouse Club

#
#Change Spring Boot embedded server port
#
server.port=7070

#
#Set the context path of the application
#
#All requests should be prefixed with /myfirstspringbootapp
#
server.servlet.context-path=/myfirstspringbootapp